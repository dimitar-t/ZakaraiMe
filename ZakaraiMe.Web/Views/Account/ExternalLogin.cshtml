@model ExternalLoginViewModel
@{
    ViewData["Title"] = WebConstants.RegisterPageTitle;
}

<h2>@ViewData["Title"]</h2>
<h4>Свържете вашия @ViewData["LoginProvider"] профил.</h4>
<hr />

<p class="text-info">
    Успешно се регистрирахте чрез <strong>@ViewData["LoginProvider"]</strong>.
    Моля въведете Имейл адрес и телефонен номер за zakaraime.com и цъкнете на бутона "Регистрация".
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-action="ExternalLoginConfirmation" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
            <div class="form-group">
                <label asp-for="Email"></label>
                <input asp-for="Email" required class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PhoneNumber"></label>
                <input asp-for="PhoneNumber" type="text" minlength="10" maxlength="14" required class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Username" />
            <input type="hidden" asp-for="ProfilePictureBytes" />
            <button type="submit" class="btn btn-default">Регистрация</button>
        </form>
    </div>
</div>